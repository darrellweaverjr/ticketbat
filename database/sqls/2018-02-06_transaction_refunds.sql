#CREATE TABLE `transaction_refunds` (
  `id` INT UNSIGNED NOT NULL AUTO_INCREMENT,
  `purchase_id` INT(10) NOT NULL,
  `user_id` INT(10) NOT NULL,
  `amount` DECIMAL(10,2) NOT NULL,
  `description` TINYTEXT NULL DEFAULT NULL,
  `type` VARCHAR(45) NULL DEFAULT NULL,
  `key` INT NULL DEFAULT NULL,
  `ref_num` INT NULL DEFAULT NULL,
  `authcode` INT NULL DEFAULT NULL,
  `is_duplicate` VARCHAR(1) NULL DEFAULT NULL,
  `result_code` VARCHAR(1) NULL DEFAULT NULL,
  `result` VARCHAR(45) NULL DEFAULT NULL,
  `error_code` INT NULL DEFAULT NULL,
  `error` VARCHAR(100) NULL DEFAULT NULL,
  `created` TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`));